# 创建新的模块测试案例
1. 获取模块支持tool列表

2. 针对每一个tool生成对应的测试用例，每次都检查是否满足，不满足请调整测试脚本
    - 测试顺序按先增加，后查询，再修改，最后删除的原则进行
    - 如果涉及其它模块信息
        - 可以调用其它模块tool获取数据
        - 如果必要也可以调用增加数据保证测试

3. 执行测试， EXEC-TEST.md 中的顺序执行测试案例