设计新的模块语言，先给出语言的基本语法，给出案例
- 参考config/services 下所有的模块案例
- 结合定义 src/config/service.rs
- 加载src/config/modular_service_loader.rs
- 重新设计一门语言，降低模块描述的复杂性，尽量介绍编写代码的行数
- 给出完整的方案，以user模块给出语言编写案例，要求案例完整
- 新语言必须支持所有的功能，不能缺少任何功能
- 新语言必须支持所有的配置项，不能缺少任何配置项
- 新语言必须支持所有的方法，不能缺少任何方法
- 新语言必须支持所有的类型，不能缺少任何类型
- 支持类型的嵌套及引用

下面完成ZML语言的实现
1. 根据现有文档档案 docs/ 设计ZML语言的语法
2. 参考模块原始案例 config/services/story/ 设计ZML语言的语法
3. 参考语法案例 example/*.zml
2. 实现ZML语言的解析器
3. 实现ZML语言的编译器
4. 测试ZML语言的实现